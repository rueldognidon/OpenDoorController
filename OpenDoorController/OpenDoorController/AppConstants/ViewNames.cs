using System;
using System.Collections.Generic;
using System.Text;

namespace OpenDoorController.AppConstants
{
    public class ViewNames
    {
        public const string LoginPage = nameof(LoginPage);
        public const string NextPage = nameof(NextPage);
    }
}
